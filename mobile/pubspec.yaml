name: capifit_mobile
description: Aplicativo Flutter para a plataforma Capifit
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  google_fonts: ^6.2.1
  provider: ^6.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/
