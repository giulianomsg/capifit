export interface TrainerProfile {
  id: string;
  userId: string;
  specialties: string[];
  certifications: string[];
  hourlyRate: number;
  bio: string;
  rating: number;
  totalStudents: number;
  createdAt: string;
}
